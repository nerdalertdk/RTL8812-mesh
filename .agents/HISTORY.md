# Imported rtw88 development history

The complete `.agents` record from the broader `rtw88` development worktree was
imported on 2026-07-27 into:

`.agents/history/rtw88-2026-07-27/`

It is an evidence/history archive, not a second active backlog. The historical
source head was `a56bcd2` (`8821a/8812a: Clear BIT_BT_PTA_EN only for USB`).
The focused package source began from that worktree and later added focused
DKMS packaging, release gates, USB control serialization, recovery hardening,
and eight-hour endurance evidence.

Use the live `.agents/PROJECT.md`, `.agents/ARCHITECTURE.md`, `.agents/TODO.md`,
and `.agents/SESSION.md` for current decisions. Consult the imported record for
the original mesh audit, Pi procedures, RTL8192FU experiment, early soak data,
upstream patch rationale, and parked OpenWrt/OpenMANET plans.
