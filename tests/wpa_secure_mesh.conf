# Legacy filename retained for existing test hosts. Keep synchronized with
# wpa_sae_mesh.conf. This credential is public test data, not a deployment key.
ctrl_interface=/run/wpa_supplicant-rtw88-mesh

network={
	ssid="rtw88-secure-mesh"
	mode=5
	frequency=2412
	key_mgmt=SAE
	psk="rtw88-mesh-test-2026"
	ieee80211w=2
	mesh_fwding=1
}
