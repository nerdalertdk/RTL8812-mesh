# Test-only IEEE 802.11s SAE/AMPE profile for hardware-in-loop validation.
# Do not reuse this credential for a deployed mesh.
ctrl_interface=/run/wpa_supplicant-rtw88-mesh

network={
	ssid="rtw88-secure-mesh"
	mode=5
	frequency=2412
	key_mgmt=SAE
	psk="rtw88-mesh-test-2026"
	mesh_fwding=1
}
